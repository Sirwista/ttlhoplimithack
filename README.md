# ttlhoplimithack
A Windows batch utility for managing TTL (Time To Live) hop values via Registry and Netsh. Enables mobile hotspot tethering bypass by adjusting IPv4/IPv6 DefaultTTL. Features auto-startup via Task Scheduler, DNS flush, connection diagnostics, and config persistence. Developed for network configuration research.
